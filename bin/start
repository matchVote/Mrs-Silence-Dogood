#!/bin/bash

# This is needed because Heroku changes the user after docker image is built
if [ $DOGOOD_ENV == "production" ]; then
  echo "Downloading NLTK punkt..."
  python -m nltk.downloader punkt
fi

echo "Starting Mrs. Silence Dogood..."
exec python -m dogood.app
