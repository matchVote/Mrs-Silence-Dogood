version: "3"
services:
  dogood:
    build: .
    command: bin/start
    env_file: .env
    environment:
      FEEDER_ENV: dev
    volumes:
      - .:/usr/src/app
    depends_on:
      - postgres

  postgres:
    image: postgres:9.6.1
    ports:
      - "5443:5432"
